let sessionToken=null;const $=(id)=>document.getElementById(id);async function initLiff(){const status=$("liffStatus");try{const config=await fetch("/api/config").then((res)=>res.json());document.title=config.app_name||"ワンさんツール";if(!config.liff_id){status.textContent="LIFF未設定 / Web表示";return;}if(!window.liff){status.textContent="LIFF SDK未読込";return;}await window.liff.init({liffId:config.liff_id});status.textContent=window.liff.isInClient()?"LINE内で起動中":"外部ブラウザ";}catch(error){status.textContent="LIFF初期化失敗";console.error(error);}}async function authenticate(){const noteId=$("noteId").value;const button=$("authButton");button.disabled=true;$("authMessage").textContent="認証中です...";try{const response=await fetch("/api/session",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({note_id:noteId})});if(!response.ok)throw new Error("認証に失敗しました");const data=await response.json();sessionToken=data.token;$("authMessage").textContent=`${data.note_id} で認証しました。`;$("chatCard").classList.remove("hidden");}catch(error){$("authMessage").textContent=error.message;}finally{button.disabled=false;}}function renderResult(result){const c=result.calculation||{};$("summaryBox").innerHTML=`<div><b>適用容積率</b><span>${c.applied_far_percent??"-"}%</span></div><div><b>最大建築面積</b><span>${c.max_footprint_m2??"-"}㎡</span></div><div><b>最大延床面積</b><span>${c.max_total_floor_area_m2??"-"}㎡</span></div><div><b>想定階数</b><span>${c.estimated_floors??"-"}階</span></div><div><b>想定戸数</b><span>${c.estimated_units??"-"}戸</span></div>`;const steps=((result.calculation_basis||{}).steps)||[];$("basisBox").innerHTML=steps.map((s,i)=>`<article class="basis-step"><h4>${i+1}. ${s.title}</h4><p><b>式:</b> ${s.formula}</p><p><b>代入:</b> ${s.substitution}</p><p><b>結果:</b> ${s.result}</p><p class="muted"><b>補足:</b> ${s.comment||"-"}</p></article>`).join("");$("resultJson").textContent=JSON.stringify(result,null,2);}async function analyze(){const file=$("fileInput").files[0];if(!sessionToken){alert("先にnote ID認証をしてください。");return;}if(!file){alert("ファイルを選択してください。");return;}const button=$("analyzeButton");button.disabled=true;button.textContent="OCRを実行しています...";const form=new FormData();form.append("token",sessionToken);form.append("file",file);form.append("manual_text",$("manualText").value);try{const response=await fetch("/api/analyze",{method:"POST",body:form});if(!response.ok)throw new Error("判定に失敗しました");const data=await response.json();renderResult(data.result);$("csvLink").href=data.exports.csv;$("xlsxLink").href=data.exports.xlsx;$("txtLink").href=data.exports.txt;$("resultCard").classList.remove("hidden");}catch(error){alert(error.message);}finally{button.disabled=false;button.textContent="OCRとボリューム判定を実行";}}$("authButton").addEventListener("click",authenticate);$("analyzeButton").addEventListener("click",analyze);initLiff();